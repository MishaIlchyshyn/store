<h1><%= t '.title' %></h1>

<%= link_to "New Product", new_store_product_path %>

<div style="margin-top: 20px", id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, store_product_path(product.id) %>
    </div>
  <% end %>
</div>
